%!TEX root = research-strategy.tex
% Approach intro — PI / collaborators / datasets / SABV / data sharing.
% \input'd into approach.tex and research-strategy.tex; do not add a document
% environment or preamble here.

\textbf{PI <Last, First> (<Title>, <Department>, <Institution>)} has a background
in % TODO: brief PI background and prior contributions, with citations to your
% own prior work \cite{KEY}.
\lipsum[9]

% TODO: Collaborator paragraph(s). One sentence per collaborator describing
%   their expertise and contribution to this proposal, ending with "(see LOS
%   <Lastname>)". Example pattern:
% <Collaborator Name> (<Institution>) is a <role> who works on <areas>
% \cite{KEY} (see LOS <Lastname>);

\textbf{Datasets.}
% TODO: Name the datasets you will use (biobanks, consortia, public datasets),
%   with citations \cite{KEY}.

\textbf{Sex as a Biological Variable.}
% TODO: State how sex will be handled. Two common patterns:
%   (1) Analyses conducted separately by sex where sample sizes permit, with
%       sex-stratified results reported alongside combined analyses.
%   (2) Sex included as a covariate / sex-specific models tested, with
%       justification.

\textbf{Data Sharing.}
% TODO: Briefly state where software, summary statistics, and analysis
%   pipelines will be deposited (GitHub, GWAS Catalog, Zenodo, etc.) and
%   under what licenses. The full DMS Plan in support/data-management.tex
%   contains the formal text.
